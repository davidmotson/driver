<Context>
 
 
<!--

DROP TABLE IF EXISTS `users`;
		
CREATE TABLE `users` (
  `id` INTEGER NULL AUTO_INCREMENT DEFAULT NULL,
  `email` VARCHAR(50) NOT NULL,
  `password` VARCHAR(50) NOT NULL,
  `phonenumber` VARCHAR(10) NOT NULL,
  `ubertoken` VARCHAR(100) NOT NULL,
  `lyfttoken` VARCHAR(100) NOT NULL ,
  `flywheelpass` VARCHAR(20) NOT NULL ,
  `sidecarpass` VARCHAR(20) NOT NULL,
  PRIMARY KEY (`id`)
);

DROP TABLE IF EXISTS `favorites`;
		
CREATE TABLE `favorites` (
  `id` INTEGER NULL AUTO_INCREMENT DEFAULT NULL,
  `user_id` INTEGER NOT NULL DEFAULT 0,
  `lat` DOUBLE NOT NULL DEFAULT 0,
  `long` DOUBLE NOT NULL DEFAULT 0,
  `name` VARCHAR(50) NOT NULL DEFAULT 'NULL',
  PRIMARY KEY (`id`)
);


ALTER TABLE `favorites` ADD FOREIGN KEY (user_id) REFERENCES `users` (`id`);

-->
 
 

  <Resource name="jdbc/driver" auth="Container" type="javax.sql.DataSource"
               maxActive="50" maxIdle="30" maxWait="10000"
               username="driver" password="password1"
               driverClassName="com.mysql.jdbc.Driver"
               url="jdbc:mysql://127.0.0.1:3306/driver"/>
 
</Context>